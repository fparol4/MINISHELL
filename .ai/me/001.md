nice, but follow the structure

lexer
	scanner
		load.c [load_init, load_free (if required)]
		move.c [move_advance, move_until, move_markstart, move_isend]
		get.c [scanner_current, scanner_peek, scanner_extract]
		table.c [scanner_tablechar, scanner_tabletoken]
		scanner.h (internal header for lexer import only)
	rules.c [rules_init (public), + (static) define_type, define_rules]
	extract.c [extract_getword, extract_getoperator]
	token.c [token_free, token_add]
	state.c [state_machine]
	lexer.c [lexer]
	lexer.h (internal header of lexer functions only: rules, extract)

take_while -> scanner_until = same function
lexer = lexer_control
