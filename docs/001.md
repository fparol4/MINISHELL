Right be confused. 
Right to have difficulties. 
Right for trying. 
Right for not trying. 
Right for understanding.
Right for avoiding. 
Right for everything. 
Right. 
--- 

We want to 
 
- [ ] Write a hashmap function 
    - [ ] fn_hashmap();
    - [ ] fn_hashmap_set(*hashmap, *key, *value);
    - [ ] fn_hashmap_get(*hashmap, *key);
    - [ ] fn_hashmap_clear(*hashmap);

- [ ] Write environment functions
    We should basically wrap the hashmap for a program `export`, `unset`
    - [ ] fn_env(*env_hashmap);
    - [ ] fn_env_set(*env_hashmap, *key, *value);
    - [ ] fn_env_get(*env_hashmap, *key, *value);
    - [ ] fn_env_clear(*env_hashmap, *key, *value);

--- 

# Writing a hashmap 

CHECKPOINT (#1)
 - [ ] (0/60m) Write a hash function that takes a string, multiplies and sums it for
 every character with a prime number (let's take 127). This is responsible for mapping to the correct path. 
 - [ ] Use the libft/linked-list and review if there is need to change or update some function. 

